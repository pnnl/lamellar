Follow instructions on skills.md, I have a serial loop that builds a workflow per matrix, runs several
computations on each. Parallelize it. It is in serial_worlkflow.rs -- but the example there has a small matrices, make it big enough so the parallelization makes sense. No i don't want you to make any changes to serial_workflow but you can make changes in serial_to_parallel_using_skills.rs

